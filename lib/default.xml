<!-- 
    Use this to bootstrap your global scripts
    Include this as the first #BGCHANGES at beat 0 in your simfile, have fun!
-->
<ActorFrame Condition="(function() 
local where=''
local function from(s) where=s..'.' end
local function load(s,...)
    local f=string.sub(GAMESTATE:GetCurrentSong():GetSongDir(), 2)..string.gsub(string.lower(where..s),'%.','/')..'.lua'
    Trace('[Loading] '..f)
    return assert(loadfile(f))(unpack(arg))
end

-- -- -- --  Add your scripts here  -- -- -- -- -- 

from 'lib'
load 'libactor'

-- -- -- -- -- -- -- -- -- -- -- -- -- -- -- -- --
return false end)()"></ActorFrame>
